import variables from '../../variables'

const card = {
  baseStyle: {
    root: {
      borderRadius: variables.borderRadiusMd,
    },
  },
  variants: {
    outline: {
      root: {
        borderRadius: variables.borderRadiusMd,
      },
    },
  },
  sizes: {
    md: {
      root: {
        borderRadius: variables.borderRadiusMd,
      },
    },
  },
}

export default card
