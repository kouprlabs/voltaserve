import variables from '@/theme/variables'

const modal = {
  baseStyle: {
    dialog: {
      borderRadius: variables.borderRadius,
    },
    closeButton: {
      borderRadius: '50%',
    },
  },
}

export default modal
