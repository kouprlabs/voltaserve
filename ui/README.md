# Voltaserve UI

Install dependencies:

```shell
npm i --legacy-peer-deps
```

Run for development:

```shell
npm run dev
```

Build Docker Image:

```shell
docker build -t voltaserve/ui .
```

Run code linter:

```shell
golangci-lint run
```
