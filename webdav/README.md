# Voltaserve WebDAV

Install dependencies:

```shell
npm i --legacy-peer-deps
```

Run for development:

```shell
npm run dev
```

Run for production:

```shell
npm run start
```

Build Docker image:

```shell
docker build -t voltaserve/webdav .
```
