# Voltaserve WebDAV

Install dependencies:

```shell
pnpm install
```

Run for development:

```shell
pnpm dev
```

Run for production:

```shell
pnpm start
```

Build Docker image:

```shell
docker build -t voltaserve/webdav .
```
