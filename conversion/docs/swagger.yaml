basePath: /v2
definitions:
  core.PipelineRunOptions:
    properties:
      bucket:
        type: string
      key:
        type: string
      size:
        type: integer
      snapshotId:
        type: string
    type: object
  core.ToolRunOptions:
    properties:
      args:
        items:
          type: string
        type: array
      bin:
        type: string
      stdout:
        type: boolean
    type: object
  errorpkg.ErrorResponse:
    properties:
      code:
        type: string
      message:
        type: string
      moreInfo:
        type: string
      status:
        type: integer
      userMessage:
        type: string
    type: object
info:
  contact: {}
  title: Voltaserve Conversion
  version: 2.0.0
paths:
  /health:
    get:
      description: Get Health
      operationId: get_health
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
        "503":
          description: Service Unavailable
          schema:
            $ref: '#/definitions/errorpkg.ErrorResponse'
      summary: Get Health
      tags:
      - Health
  /pipelines/run:
    post:
      consumes:
      - application/json
      description: Run
      operationId: pipeline_run
      parameters:
      - description: Body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/core.PipelineRunOptions'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/errorpkg.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/errorpkg.ErrorResponse'
      summary: Run
      tags:
      - Pipelines
  /tools/run:
    post:
      consumes:
      - application/json
      description: Run
      operationId: tools_run
      parameters:
      - description: Body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/core.ToolRunOptions'
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/errorpkg.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/errorpkg.ErrorResponse'
      summary: Run
      tags:
      - Tools
swagger: "2.0"
